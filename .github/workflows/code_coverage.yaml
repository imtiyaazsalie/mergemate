name: Code Coverage

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt -r requirements-dev.txt
      - run: pip install pytest-cov && PYTHONPATH=. pytest tests/unittest --cov=mergemate.core --cov=mergemate.agent --cov=mergemate.tools.base --cov=mergemate.tools.pr_reviewer --cov-report=xml --cov-report=term
      - uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: core
          files: coverage.xml
